<Window x:Class="LabWork3Task2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork3Task2"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel Margin="5" Orientation="Horizontal">
            <TextBlock Text="Поиск:" Margin="0 0 5 0"/>
            <TextBox Width="300"/>
            <Button Content="Найти" Margin="5 0 0 0"/>
        </StackPanel>

        <DataGrid ItemsSource="{Binding Users}" Grid.Row="1" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Код" Binding="{Binding Id}"/>
                <DataGridTextColumn Header="Логин" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Пароль" Binding="{Binding Password}"/>
                <DataGridTextColumn Header="Почта" Binding="{Binding Email}"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <StackPanel Margin="5" Grid.Row="2" Orientation="Horizontal">
            <Button Content="Экспорт" Margin="0 0 5 0" Command="{Binding ProcessResponseCommand}"/>
            <Button Content="Импорт" Margin="5 0 0 0"/>
            <RadioButton Content="По убыванию"/>
            <RadioButton Content="По возрастанию"/>
        </StackPanel>
        
    </Grid>
</Window>
